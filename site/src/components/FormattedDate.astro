---
interface Props { date: Date; }
const { date } = Astro.props;
const f = new Intl.DateTimeFormat('ko-KR', { year: 'numeric', month: 'long', day: 'numeric' });
---
<time datetime={date.toISOString()}>{f.format(date)}</time>
